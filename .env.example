# Produktions-URL der Anwendung (wichtig für E-Mail-Links)
NEXT_PUBLIC_BASE_URL=https://www.basar-neukirchen.de

# Datenbank-URL (für Prisma)
# Beispiel für PostgreSQL:
# DATABASE_URL="postgresql://user:password@localhost:5432/kinderbasar"
DATABASE_URL=

# E-Mail Konfiguration
EMAIL_FROM=noreply@basar-neukirchen.de
# Für Entwicklung: setzen Sie EMAIL_PROVIDER=console
# Für Produktion: konfigurieren Sie Ihren SMTP-Server
EMAIL_PROVIDER=

# SMTP Konfiguration (wenn EMAIL_PROVIDER=smtp)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=false
